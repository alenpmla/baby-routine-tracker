# Phase 4 addendum — Edit profile moved into Settings

## Goal

Move **Edit profile** out of the Dashboard header into **Settings** as a sub-screen (like Units, Data & reports, Food suggestions).

## Acceptance criteria

- The Dashboard header no longer shows "Edit profile" (keeps the Settings gear).
- Settings gains an "Edit profile" nav row → a sub-screen with the profile form (name, DOB, notes), a leading M3 back arrow, and Save returning to Settings.
- Build + tests pass.
